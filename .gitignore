# Environment variables (all variants may hold credentials, except the examples)
.env*
!.env.example
!.env.dav.example

# Runtime files (pid, logs, nginx temp dirs)
target/*
!target/.keep

# make init build artifacts (never commit compiled libs — see README security philosophy)
bin/lxp.so
bin/lxp/lom.lua

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
.tmp/
